using FactoryPattern.Patterns.Factory;

// ────────────────────────────────────────────────────────────────
// Program.cs — The starting point
//
// This is where the application begins. It calls FactoryDemo.Run()
// which runs all four factory pattern examples one by one.
// The output shows how each pattern creates and uses objects.
// ────────────────────────────────────────────────────────────────
FactoryDemo.Run();
